# Site Quality Issues

Generated by evaluator audit on 2026-04-12.

## P0 — Critical

1. **9 of 11 content pages are empty stubs** — B-trees, HNSW, Inverted Index, Merkle Tree, CRDT, Bloom Filter, lock-free structures, ConcurrentHashMap, immutable/persistent structures, graphs are all blank. B-trees — FIXED (validated 2026-05-23): полноценная страница ~390 строк в `docs/search/trees-and-graphs/b-trees.mdx` с мотивацией (RAM vs disk latency), параметрами, Python-псевдокодом поиска/вставки, PostgreSQL-применением, ECharts; неоднократно правлена через fact-check pass. HNSW — FIXED (2026-04-22): full page published at `docs/search/vector-indexes-and-quantization/hnsw`. Inverted Index — FIXED (2026-04-24): full page published at `docs/search/text-indexes/inverted-index.mdx` (~538 lines) with dictionary + postings, SPIMI/BSBI, AND/skip/phrase/wildcard search, TF-IDF, BM25 with Lucene IDF variant, ECharts TF-saturation chart, gap/VByte/gamma/PFOR compression, Lucene/ES/PG GIN practice, references. Merkle Tree — FIXED (2026-04-26): full page published at `docs/search/hashing/merkle-tree.mdx` (~412 lines) with motivation, structure, inclusion proofs (RFC 6962 prefixes), construction algorithms, security (CVE-2012-2459), applications (Git, Bitcoin, Ethereum, Cassandra anti-entropy, Certificate Transparency, IPFS), variants (Merkle Patricia Trie, Sparse Merkle Trees, Verkle), ECharts proof-size growth chart, references. Lock-free структуры данных — FIXED (2026-05-08): full page published at docs/concurrent/lock-free/concurrent-data-structures.mdx with motivation, progress conditions, atomic primitives, ABA & memory reclamation, Treiber/Michael-Scott/Harris/hash/skiplist/Bw-tree, hazard pointers / EBR / RCU, DB & distributed applications, ECharts contention chart, references; fact-check follow-up FIXED (2026-05-18): items #1.1..#1.5 corrected (Pathfinder wording, ConcurrentSkipListMap attribution, NetBSD pserialize(9), contention-chart illustrative note + 14-CPU marker, reference [19] repointed to xlog.c). ConcurrentHashMap — FIXED (2026-06-07): full page published at `docs/concurrent/java/concurrent-hash-map.mdx` (~407 lines) with motivation, API/contract, evolution (segmented→Java 8 rewrite), Java 8+ mechanisms (constants, hash-markers, bin lifecycle, resize/ForwardingNode, baseCount+CounterCell, compose-ops), size()/mappingCount() semantics, weakly consistent iterators, pseudocode, ConcurrentSkipListMap comparison, complexity, DB/distributed applications (Caffeine, Kafka, Cassandra, Spring), ECharts throughput chart (illustrative-disclaimer), pitfalls, decision matrix, references; fact-checked (`fact-check/concurrent-hash-map.md`); deploy run 27096736952 SUCCESS. Fact-check follow-up FIXED (2026-06-07): size()→mappingCount() quote re-attribution; JDK ticket attribution corrected (JDK-8161372/JDK-8172951 → JDK-8062841/JDK-8074374); 9.1 lowercase-"map" confirmed already correct vs Java 21 javadoc; ref [8] Hendler-Shavit retained as in-body disclaimer-template cross-reference. Bloom Filter — FIXED (2026-06-07): full page published at `docs/distributed/probabilistic/bloom-filter.mdx` (~366 lines, renamed from 0-byte .md stub) with motivation (AMQ, error asymmetry, m/n≈1.44·log2(1/p)), construction + Python pseudocode, worked numeric example (m=10,k=3, TP/FP/TN queries), FPP analysis (k*=(m/n)ln2, m≈−1.44·n·log2 p, Bose 2008 underestimation caveat), double hashing (Kirsch–Mitzenmacher, asymptotic-FPP wording), counting/scalable/blocked/partitioned variants, AMQ comparison (Cuckoo/Quotient/XOR/Ribbon), applications (LSM/SSTable Bigtable §Refinements, Bitcoin BIP-37, PostgreSQL bloom, web caches/CDN, Chrome Safe Browsing hedged), complexity, ECharts FPP-vs-(m/n) chart (all 60 points recomputed against formula, labelled "значения вычислены по формуле"), 16 references (all DOIs/URLs resolve); fact-checked (`fact-check/bloom-filter.md`, 0 опровергнуто / 0 неточно / 0 неподтверждено); deploy run 27101640199 SUCCESS, live page verified (H1, chart canvas non-blank, references present). CRDT — FIXED (2026-06-08): full page published at `docs/distributed/crdt.mdx` (renamed from 0-byte `cdrt.md` stub via `git mv`, also closes #17) with 15 sections — мотивация (AP/CAP, слабости LWW/ручного разрешения), модель системы + SEC, две семьи CvRDT (state-based, join-semilattice/LUB, монотонность, comm/assoc/idem) и CmRDT (op-based, конкурентные коммутируют + reliable causal delivery), теорема сходимости (Shapiro/Preguiça/Baquero/Zawirski 2011), счётчики (G-Counter, PN-Counter — арифметика пересчитана независимо: 11 инкрементов, P−N=9), множества (G-Set/2P-Set/LWW-Element-Set/OR-Set add-wins — пример observed-remove {a1} пересчитан), регистры (LWW/MV), версионирование (vector clocks/version vectors/DVV), sequence CRDT (WOOT/Treedoc/Logoot/RGA/YATA-Yjs/Automerge), delta-state CRDT (Almeida/Shoker/Baquero 2018), tombstones+GC (каузальная стабильность), реальные системы (Riak DT/Redis Active-Active/Cosmos DB/Automerge-Yjs/Akka/Cassandra — сверены с офиц. докой), когда CRDT не подходят vs консенсус, ECharts state-vs-delta трафик-чарт (иллюстративный дисклеймер), 16 ссылок (все DOI/URL резолвятся); fact-checked (`fact-check/crdt.md`, 0 опровергнуто / 0 неточно / 0 неподтверждено, 1 косметика → #30); deploy run 27109927082 SUCCESS, live page verified (H1, 15 H2, chart canvas 748×380 non-blank, references present, /cdrt→404, /crdt→200). Immutable/persistent structures — FIXED (2026-06-08): full page published at `docs/concurrent/immutable/immutable-vs-persistent.md` (kept .md, no chart — структурно-алгоритмическая тема) with 14 sections — мотивация (lock-free чтение снимков vs RMW lock-free страницы), определения immutability vs persistence + таксономия (partial/full/confluent/functional, DSST), структурное разделение (path copying O(log n) vs наивный COW O(n)), методы DSST (fat node/path copying/node copying), персистентные списки и стеки (cons-list, пример пересчитан: 4 узла = 3 версии), сбалансированные деревья (RB/weight-balanced path copying, OCaml/Haskell Map), HAMT (Bagwell 2001, ветвление 32, bitmap+popcount, глубина ceil(log32 10^6)=4 пересчитана), персистентные векторы (Clojure bit-partitioned trie, 32^4≥10^6 ⇒ глубина 4, RRB-Trees Bagwell&Rompf 2011), finger trees (Hinze&Paterson 2006), амортизация и решение Окасаки (multiple futures ломают амортизацию → ленивость+мемоизация+дебеты), связь с БД (MVCC, CouchDB append-only B+tree, LMDB COW B+tree, LSM SSTable), сборка мусора/refcounting, сводная таблица сложности, 8 ссылок (все DOI/URL/EPFL-записи резолвятся); fact-checked (`fact-check/immutable-vs-persistent.md`, 0 опровергнуто / 0 неточно / 0 неподтверждено, 1 косметика → #31); deploy run 27110276803 SUCCESS, live page verified (H1, таблицы, обратная ссылка с lock-free страницы резолвится). Graphs (compact representations) — FIXED (2026-06-08): full page published at `docs/compact/trees-and-graphs/graphs.mdx` (renamed from 0-byte `graphs.md` stub via `git mv`) with 13 блоков — мотивация (веб-графы 10^10–10^11 дуг, ~30 бит/ребро наивно, в scope: представления, не BFS/DFS/Дейкстра), инф.-теор. граница (LB=log2 C(n^2,m), аппроксимация m·log2(n^2/m), числа 13.29/14.73 бит/ребро пересчитаны независимо, разность=log2 e, трихотомия implicit/succinct/compact), bit-vectors rank/select (Джекобсон [1]/Кларк [2]/RRR [3]), сукцинктные деревья (LOUDS/BP/DFUDS 2n+o(n), range min-max Наварро–Садаканэ [5]), компактные графы (a–e: bitmap+rank/select, k²-деревья [6][7], WebGraph [8], Re-Pair [10], планарные/сепараторные), RDF/k²-triples [9], таблица запросов/стоимости, worked example k²-дерево (рёбра {0→1,2→3,3→2}, T=1001 / L=0100 0110 — пересчитано побитно независимо), применения БД/распределённые, ECharts бит/ребро чарт (load-bearing цифры 3.08 WebGraph и 3.3–5.3 k²-дерево трассированы к первоисточникам; наивный/граница помечены иллюстративными), таблица сложности, 11 ссылок (все DOI/метаданные верны и резолвятся); fact-checked (`fact-check/graphs.md`, 0 опровергнуто / 0 неточно / 0 неподтверждено); deploy run 27138709615 SUCCESS, live page verified (H1, 12 H2 + подразделы a–e, chart canvas 748×380 non-blank/283775 px, 4 таблицы, 21 inline ref-ссылка / 0 битых, frontmatter title, no LaTeX, в scope). **С этим P0#1 ПОЛНОСТЬЮ РЕШЁН — все 10 страниц-заглушек (B-trees, HNSW, Inverted Index, Merkle Tree, Lock-free, ConcurrentHashMap, Bloom Filter, CRDT, immutable/persistent, graphs) теперь FIXED.**
2. ~~**`congratulations.md` is live**~~ — Docusaurus template page published on the course site. Must be removed. — **FIXED (2026-06-09)**: `docs/congratulations.md` deleted (submodule commit 1165018); `/docs/congratulations` → 404 verified live (deploy run 27176308307 SUCCESS).
3. ~~**All `_category_.json` descriptions are Docusaurus boilerplate**~~ — `"5 minutes to learn the most important Docusaurus concepts."` appears on every category page. Must be replaced with real descriptions. — **FIXED (2026-06-09)**: all 14 `_category_.json` descriptions rewritten as accurate Russian prose matching their section contents; grep for "5 minutes to learn" across the submodule returns no matches; `/docs/category/concurrent` description verified live.

## P1 — Serious

4. ~~**R-tree complexity table is academically imprecise**~~ — **FIXED**: search complexity now shown as "not guaranteed", with explanation that O(log_M n) is empirical for R*-tree, not theoretical. References added.
5. ~~**R*-tree reinsertion description omits key detail**~~ — omits that the 30% selected for reinsertion are the entries furthest from the node centroid. — **FIXED (2026-06-09)**: `docs/search/spatial-indexes/r-tree.mdx` строка 212 теперь явно описывает алгоритм ReInsert (Beckmann 1990): для всех записей вычисляется расстояние центра их MBR до центра MBR переполнившегося узла, записи сортируются по убыванию расстояния, удаляются первые `p≈30%` — самые удалённые от центроида — и переустанавливаются («close reinsert», начиная с ближайших). Fact-checked против Beckmann et al. 1990 (`fact-check/r-tree.md` строка 14, ✅ ПОДТВЕРЖДЕНО). Submodule commit 897b8be, deploy run 27177125167 SUCCESS; «самые удалённые от центроида» + «close reinsert» верифицированы на проде через Playwright.
6. ~~**MBR record size uses 4-byte pointer**~~ — **FIXED (2026-04-22)**: pointer updated to 8 bytes (record ≈ 40 B), estimate explicitly labeled as illustrative.
7. ~~**PostGIS page is too shallow**~~ — lab worksheet level, not course material. Lacks explanation of two-phase filter/refine, GiST operator classes, when the planner won't use the index. — **FIXED (2026-06-09)**: `docs/search/spatial-indexes/postgis.mdx` расширена концептуальными разделами поверх сохранённой лабораторной — двухфазная модель filter/refine, GiST как обобщённый фреймворк, классы операторов (opclasses) с таблицей, KNN (`<->`), причины отказа планировщика от индекса (a–e, включая `ST_Distance` vs `ST_DWithin`), альтернативы SP-GiST/BRIN, модель стоимости, 9 ссылок с anchor-id. Fact-checked построчно (`fact-check/postgis.md`, 34 ✅ / 0 ❌ после исправления). В ходе аудита найдена и исправлена 1 фактическая ошибка: статус обязательности GiST support-method `same`/`compress` был инвертирован относительно цитируемой PG-доки — исправлено (submodule commit 4ebc619). Базовое расширение: submodule commit 6430f40; итоговый деплой run 27206878347 SUCCESS. Live-verified via Playwright (H1, 8 новых H2 в нужном порядке, Список литературы, ./r-tree.mdx линк резолвится, исправленный список support-methods).
8. **Empty stub page titles are URL slugs** — pages lack YAML frontmatter `title:` so titles render as `b-trees`, `hnsw`, `cdrt`, etc. HNSW — FIXED (2026-04-22): new `hnsw.mdx` has `title: HNSW` in frontmatter. Merkle Tree — FIXED (2026-04-26): new `merkle-tree.mdx` has `title: Дерево Меркла` in frontmatter. Lock-free structures — FIXED (2026-05-08): new `concurrent-data-structures.mdx` has `title: Lock-free и wait-free структуры данных` in frontmatter. ConcurrentHashMap — FIXED (2026-06-07): new `concurrent-hash-map.mdx` has `title: ConcurrentHashMap в Java` in frontmatter. Graphs — FIXED (2026-06-08): new `graphs.mdx` has `title: Компактные представления графов` in frontmatter.

## P2 — Navigation / UX

9. ~~**Homepage CTA links only to Search category**~~ — skips Concurrent, Distributed, and Compact sections. — **FIXED (2026-06-09)**: `src/pages/index.tsx` now maps over a `categories` array rendering four CTA buttons (Поиск/Конкурентность/Распределённые системы/Компактные структуры) linking to `/docs/category/{search,concurrent,distributed,compact}`; `.buttons` got `flex-wrap: wrap; gap: 0.75rem` for mobile wrapping. All four buttons + hrefs verified live; `/docs/category/compact` resolves 200. Submodule commit c9f6b70, deploy run 27207311497 SUCCESS.
10. **No introductory/overview page in any category** — no orientation, prerequisites, or framing for any section.
11. ~~**Empty pages are fully navigable**~~ — stubs appear in sidebar with no way to hide them. — **FIXED (2026-06-09)**: moot — all stub pages filled (P0#1 fully resolved); `find docs -name '*.md' -o -name '*.mdx'` shows zero empty/zero-byte content files, so no empty pages remain in the sidebar. Verified during deploy run 27207311497.
12. **No search functionality configured** — no Algolia or local search plugin.
13. ~~**Navbar title "Описание" is confusing**~~ — should be "Главная" or a course abbreviation. — **FIXED (2026-06-09)**: `docusaurus.config.ts` navbar title changed to "Главная"; old "Описание" navbar title gone (remaining "Описание" matches in repo are legitimate reference prose, not config); verified live in navbar.

## P3 — Charts

14. ~~**Radar chart values in R-tree page are undocumented and subjective**~~ — **FIXED**: chart removed.
15. **MBR scatter chart shows only one MBR with no hierarchy** — a more instructive visualization would show 2–3 groups with their own MBRs plus a parent MBR.
16. **Tree hierarchy still shown as ASCII** — should be a real chart given ECharts is already configured.
21. ~~**HNSW recall-vs-QPS chart data is fabricated**~~ — **FIXED (2026-04-23)**: option (a) — chart now uses real values from LUCENE-9937 ticket table (`(ef, recall, QPS)` = `(10, 0.713, 69663)`, `(50, 0.950, 28022)`, `(100, 0.985, 16109)`, `(500, 1.000, 4116)`, `(800, 1.000, 2730)`, verified against the ticket source), attribution tightened to the specific single-thread LUCENE-9937 run with a note that ann-benchmarks.com leaderboard values differ due to hardware/threading.

## P1 — Inverted index page (opened 2026-04-24)

22. ~~**Mermaid diagram on inverted-index page does not render as a diagram**~~ — **FIXED (2026-04-24)**: `flowchart LR` code block replaced with an HTML table showing словарь → постинг-лист; Mermaid theme not enabled to avoid regressions on unrelated pages.

23. ~~**PForDelta misattribution**~~ — **FIXED (2026-04-24)**: added Zukowski, Héman, Nes, Boncz (ICDE 2006) as `ref-zukowski-pfor` [10]; PForDelta in body now cites Zukowski et al. 2006 as the origin, Lemire & Boytsov 2015 kept only for the SIMD-decoding remark.

24. ~~**Orphan reference [10] Zobel & Moffat 2006**~~ — **FIXED (2026-04-24)**: cited in `## Мотивация` as the canonical survey of inverted files; renumbered to [11] after Zukowski insertion.

## P2 — Inverted index page polish

25. ~~**`.pay` description is slightly off**~~ — **FIXED (2026-04-24)**: wording updated to "смещения символов (character offsets)" in both the mini-example paragraph and the Lucene file-format list.

26. ~~**`russian_morphology` presented as built-in ES filter**~~ — **FIXED (2026-04-24)**: added clarifying paragraph after the analyzer JSON that `russian_morphology` comes from the third-party `elasticsearch-analysis-morphology` plugin and `ru_stopwords` is user-defined; mentions `hunspell` as a built-in alternative. Source: [Elastic analyzer anatomy](https://www.elastic.co/guide/en/elasticsearch/reference/current/analyzer-anatomy.html).

27. ~~**PostgreSQL GIN chapter number outdated**~~ — **FIXED (2026-04-24)**: reference [5] no longer hard-codes "Chapter 70" — now notes that chapter numbering varies between major versions (PG 16 = 70, PG 17/18 = 65.4); link pins to `/docs/current/gin.html`. Follow-up correction (2026-04-25, see #28): PG 17 = 64.4, not 65.4.

28. ~~**Inverted-index fact-check pass — 7 inaccuracies in content**~~ — **FIXED (2026-04-25)**: full fact-check audit (`fact-check/inverted-index.md`) flagged seven items, all corrected in a single pass: (a) Heaps' law β reduced from "0.4–0.6" to canonical Manning IIR §5.1.1 value "≈ 0.5"; (b) FST sentence rewritten so `.tip` is named as the term index pointing to the block dictionary in `.tim` (no longer conflated as "FST in .tim/.tip"); (c) BM25 attribution changed from "модель Robertson–Spärck Jones" to "семейства Okapi BM25 (Robertson, Walker, Spärck Jones и др., ~1994)"; (d) `Lucene90PostingsFormat` corrected to `Lucene99PostingsFormat` in the PFOR section (Lucene 9.7+ default); (e) Codec architecture example now lists Codecs only (`Lucene90Codec`, `Lucene94Codec`, `Lucene99Codec`) — previously mixed `PostingsFormat` with `Codec`; (f) PG GIN chapter mapping corrected to "PG 16 — 70, PG 17 — 64.4, PG 18 — 65.4" (was "PG 17/18 — 65.4"); (g) Robertson & Zaragoza 2009 bibliography volume corrected to "4(1–2), 1–174" (FnTIR vol. 4, was incorrect). All fixes verified on the live page after deploy run 24925818358.

## P1 — Merkle Tree page (opened 2026-04-27)

29. ~~**Merkle Tree fact-check pass — 2 опровергнутых факта + 6 неточностей**~~ — **FIXED (2026-04-27)**: full fact-check audit (`fact-check/merkle-tree.md`) flagged eight items, all corrected in a single pass: (a) Grover-vs-BHT — Grover gives k/2 for preimage only; collision quantum bound is BHT's 2^(n/3) ≈ 85 бит для SHA-256, formulation rewritten and BHT reference [16] added; (b) Cassandra repair URL fixed to `managing/operating/repair.html` (was 404); (c) etcd snapshot trees заменены на Trillian (etcd snapshot — это bbolt serialization, не Merkle tree); (d) Btrfs пояснение — по умолчанию CRC32C в B-дереве checksum tree, не Merkle tree; SHA-256/BLAKE2 опционально с kernel 5.5+; (e) Git SHA-256 датировка исправлена на «с 2020 года, стабильно с Git 2.42 (август 2023)» (было «с 2018 года»); (f) Verkle hard-fork — Hegota (2H 2026), Verge — этап roadmap'а; (g) Kuszmaul ref год исправлен на «(2018)» в соответствии с URL; (h) [14] SHAttered URL заменён на `eprint.iacr.org/2017/190` (shattered.io припаркован). All fixes verified on the live page after deploy run 25015736079.

## P4 — Consistency / Polish

17. ~~**`cdrt` slug is wrong**~~ — **FIXED (2026-06-08)**: `docs/distributed/cdrt.md` renamed to `docs/distributed/crdt.mdx` via `git mv`; live `/docs/distributed/crdt` → 200, old `/docs/distributed/cdrt` → 404. Verified by evaluator (deploy run 27109927082).
18. ~~**`markdown-features.mdx` is a live template page**~~ — leftover Docusaurus artifact, must be removed. — **FIXED (2026-06-09)**: `docs/markdown-features.mdx` deleted (also removed leftover `src/pages/markdown-page.md`); `/docs/markdown-features` and `/markdown-page` → 404 verified live.
19. ~~**No footer configured**~~ — at minimum a copyright notice and link to ITMO. — **FIXED (2026-06-09)**: `docusaurus.config.ts` adds a dark `footer` with two columns — «Материалы» (4 category links) and «Ссылки» (Университет ИТМО → https://itmo.ru/, GitHub → repo) — plus copyright «© {year} Университет ИТМО, Факультет программной инженерии и компьютерной техники. Сделано с помощью Docusaurus.». Both columns, ITMO link, GitHub link, copyright, and the 4 category links verified live. Submodule commit c9f6b70, deploy run 27207311497 SUCCESS.
20. ~~**Social card image may be a placeholder**~~ — `img/itmo-social-card.jpg` referenced in config; verify it is a real ITMO-branded image. — **FIXED (2026-06-09)**: verified NOT a placeholder — `static/img/itmo-social-card.jpg` is a real 91,602-byte 1197×499 baseline JPEG with official ITMO branding. No code change required. Confirmed during deploy run 27207311497 review.

## P4 — CRDT page (opened 2026-06-08)

30. ~~**CRDT reference [3] is defined but never cited inline**~~ — `<a id="ref-preguica-survey">` (Preguiça 2018, arXiv:1806.10254) appears in `## Список литературы` but has no inline `[[3](#ref-preguica-survey)]` citation in the body, unlike the bloom-filter house style where every numbered reference is cited. Cosmetic; metadata is correct and the URL resolves. — **FIXED (2026-06-09)**: inline `[[3](#ref-preguica-survey)]` added at end of the `## Две семьи CRDT` intro sentence ("…современный обзор обоих семейств и их вариантов даёт [3]."); link resolves to the anchor live; reference metadata re-verified (arXiv:1806.10254, Preguiça 2018, overview covering state-/op-/delta-state CRDTs). Submodule commit 93ea650, deploy run 27176587137 SUCCESS, verified live via Playwright.

## P4 — Immutable/persistent page (opened 2026-06-08)

31. ~~**CouchDB reference [6] title vs cited URL mismatch**~~ — `## Список литературы` entry [6] uses the title «The Power of B-trees», which physically lives at `guide.couchdb.org/draft/btree.html`, but the cited URL points to the Technical Overview (`docs.couchdb.org/en/stable/intro/overview.html`). The overview URL does support the cited fact (append-only B-tree, new root on write, crash-resistant MVCC) and resolves, so this does not block. — **FIXED (2026-06-09)**: entry [6] relabelled to «Technical Overview — append-only хранилище и устойчивые к сбоям MVCC-чтения через B+деревья (раздел «ACID Properties»)», so title now matches the cited overview URL. Re-verified live: page title is "1.1. Technical Overview", section "1.1.2. ACID Properties" states view-index updates "are always appended at the end of the file … to ensure crashes and power failures can not cause corruption of indexes"; in-body claim at line 170 still supported. Submodule commit 93ea650, deploy run 27176587137 SUCCESS, verified live via Playwright.

## P4 — R-tree page (opened 2026-06-09)

33. ~~**R-tree «Сложность»: inline-ссылка [4] резолвится на Beckmann (запись №3)**~~ — в `docs/search/spatial-indexes/r-tree.mdx` строка 243 (`[[1](#ref-guttman), [4](#ref-beckmann)]`) видимый номер «4» указывает на anchor `#ref-beckmann`, однако Beckmann et al. 1990 — это запись **№3** в списке литературы; запись №4 — Manolopoulos et al. 2006. Видимый номер ссылки не совпадал с фактически резолвящейся записью. Зафиксировано в `fact-check/r-tree.md`. — **FIXED (2026-06-09)**: строка 243 теперь `[[1](#ref-guttman), [3](#ref-beckmann)]`; видимый номер «3» совпадает с записью №3 (Beckmann et al. 1990). Verified live via Playwright: rendered text shows `[1, 3]` and link «3» → `#ref-beckmann`. Submodule commit c9f6b70, deploy run 27207311497 SUCCESS.

## P3 — CI / Infrastructure

32. ~~**Node 20 GitHub Actions runtime deprecation**~~ — deploy.yml uses actions/checkout@v4, actions/setup-node@v4, and (transitively via upload-pages-artifact@v3) actions/upload-artifact@v4, all running on the Node.js 20 actions runtime. Per GitHub's 2025-09-19 changelog, JS actions will be forced to Node.js 24 starting 2026-06-16, and Node.js 20 is removed from runners 2026-09-16. Note: the `node-version: 20` build input is a separate, unaffected project choice. Ref: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/ — **FIXED (2026-06-09)**: bumped both `deploy.yml` and `test-deploy.yml` to action majors that ship a Node 24 runtime — `actions/checkout@v6` (node24), `actions/setup-node@v6` (node24), `actions/upload-pages-artifact@v5` (composite → `actions/upload-artifact@v7`, node24), `actions/deploy-pages@v5` (node24); each runtime confirmed via the action's `action.yml` `runs.using` before bumping. Submodule commit a7f46e6, deploy run 27207611662 SUCCESS — the "Node.js 20 actions are deprecated" annotation is gone (check-run annotations_count: 0; only an unrelated `punycode` Node DeprecationWarning remains). The `node-version: 20` build input was intentionally left unchanged.
