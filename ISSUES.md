# Site Quality Issues

Generated by evaluator audit on 2026-04-12.

## P0 — Critical

1. **9 of 11 content pages are empty stubs** — B-trees, HNSW, Inverted Index, Merkle Tree, CRDT, Bloom Filter, lock-free structures, ConcurrentHashMap, immutable/persistent structures, graphs are all blank.
2. **`congratulations.md` is live** — Docusaurus template page published on the course site. Must be removed.
3. **All `_category_.json` descriptions are Docusaurus boilerplate** — `"5 minutes to learn the most important Docusaurus concepts."` appears on every category page. Must be replaced with real descriptions.

## P1 — Serious

4. **R-tree complexity table is academically imprecise** — `O(log_M n)` average case is not theoretically guaranteed for range queries in 2D+, only empirical. Should be qualified.
5. **R*-tree reinsertion description omits key detail** — omits that the 30% selected for reinsertion are the entries furthest from the node centroid.
6. **MBR record size uses 4-byte pointer** — anachronistic on 64-bit systems (should be 8 bytes). Should be labeled as an illustrative estimate.
7. **PostGIS page is too shallow** — lab worksheet level, not course material. Lacks explanation of two-phase filter/refine, GiST operator classes, when the planner won't use the index.
8. **Empty stub page titles are URL slugs** — pages lack YAML frontmatter `title:` so titles render as `b-trees`, `hnsw`, `cdrt`, etc.

## P2 — Navigation / UX

9. **Homepage CTA links only to Search category** — skips Concurrent, Distributed, and Compact sections.
10. **No introductory/overview page in any category** — no orientation, prerequisites, or framing for any section.
11. **Empty pages are fully navigable** — stubs appear in sidebar with no way to hide them.
12. **No search functionality configured** — no Algolia or local search plugin.
13. **Navbar title "Описание" is confusing** — should be "Главная" or a course abbreviation.

## P3 — Charts

14. ~~**Radar chart values in R-tree page are undocumented and subjective**~~ — **FIXED**: chart removed.
15. **MBR scatter chart shows only one MBR with no hierarchy** — a more instructive visualization would show 2–3 groups with their own MBRs plus a parent MBR.
16. **Tree hierarchy still shown as ASCII** — should be a real chart given ECharts is already configured.

## P4 — Consistency / Polish

17. **`cdrt` slug is wrong** — should be `crdt` (Conflict-free Replicated Data Type).
18. **`markdown-features.mdx` is a live template page** — leftover Docusaurus artifact, must be removed.
19. **No footer configured** — at minimum a copyright notice and link to ITMO.
20. **Social card image may be a placeholder** — `img/itmo-social-card.jpg` referenced in config; verify it is a real ITMO-branded image.
