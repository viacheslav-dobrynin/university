# Fact-check log: PostGIS

**Файл**: `courses/structures-and-algorithms-in-databases-and-distributed-systems/docs/search/spatial-indexes/postgis.mdx`

Аудит проведён 2026-06-09 (evaluator-проход по расширению глубины, ISSUES #7). Покрывает новые концептуальные разделы (lab-части не перепроверялись построчно — оставлены без изменений, тайминги 1269ms→100ms помечены как машинозависимые и не являются фабрикацией). Сверка с авторитетными источниками: PostgreSQL docs (gist.html, spgist.html, brin.html, using-explain.html), PostGIS Manual (using_postgis_dbmanagement.html, geometry_distance_knn.html, ST_DWithin.html), PostGIS Workshop (indexing.html), Hellerstein/Naughton/Pfeffer VLDB 1995, Beckmann et al. 1990.

## Построчная сверка

| # | Утверждение (строки) | Статус | Источник | Комментарий |
|---|---|---|---|---|
| 1 | Вступление: PostGIS не реализует отдельный тип индекса; опирается на штатный GiST, который для пространственных типов ведёт себя как R-дерево (12) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual Spatial Indexing [6]; PG gist.html [2] | Корректно. Отдельный тип `RTREE` удалён в PostgreSQL 8.2; пространственные индексы — это GiST/SP-GiST/BRIN. |
| 2 | Ссылка на ./r-tree.mdx во вступлении присутствует (12) | ✅ ПОДТВЕРЖДЕНО | проверено на проде | Линк резолвится в `/docs/search/spatial-indexes/r-tree`, не 404 (onBrokenLinks:'throw' + green build). |
| 3 | Пространственный индекс хранит не геометрии, а только bounding box; в индексе лежат четыре числа `(x_min, y_min, x_max, y_max)` (137) | ✅ ПОДТВЕРЖДЕНО | PostGIS Workshop indexing.html [7]; Manual [6] | Корректно — GiST для geometry хранит bbox (compress кладёт bbox). |
| 4 | Двухфазная модель: primary filter (индекс по bbox быстро находит кандидатов) + secondary filter (точный предикат только на прошедших первый тест) (139–143) | ✅ ПОДТВЕРЖДЕНО | PostGIS Workshop indexing.html [7] | Дословно соответствует формулировке воркшопа «which rectangles intersect... very quickly» + точная проверка только на кандидатах. |
| 5 | Оператор `&&` истинно, когда bounding box `a` и `b` перекрываются или соприкасаются; именно его обслуживает GiST-индекс (145) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual `&&` [6] | `&&` — "bounding boxes intersect" (включая касание). Корректно. |
| 6 | `ST_Contains`/`ST_Intersects` внутри комбинируют дешёвое `&&` по bbox и дорогой точный предикат (147–157) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual; Workshop [6][7] | Index-aware функции автоматически добавляют bbox-условие. Корректно. |
| 7 | GiST = расширяемый шаблон сбалансированного дерева поиска; механика дерева в ядре PG один раз, «значение» задаётся пользовательскими функциями (159–161) | ✅ ПОДТВЕРЖДЕНО | Hellerstein/Naughton/Pfeffer VLDB 1995 [1]; PG gist.html [2] | Корректное изложение идеи GiST. |
| 8 | GiST предложен Hellerstein, Naughton, Pfeffer на VLDB 1995 (161) | ✅ ПОДТВЕРЖДЕНО | VLDB 1995 P562.PDF [1] | Авторы, конференция, год — верны. |
| 9 | **Список опорных функций GiST: статус обязательности `compress` и `same` (163–171)** | ✅ ИСПРАВЛЕНО (было ❌ ОПРОВЕРГНУТО) | PG gist.html [2] (цитируемый источник!) | **БЫЛО:** `compress` помечена обязательной, `same` — необязательной (инверсия). Согласно официальной документации PostgreSQL обязательны `consistent`, `union`, `penalty`, `picksplit`, **`same`**; `compress` **НЕОБЯЗАТЕЛЬНА** ("If the compress method is omitted, data items are stored in the index without modification."). **FIXED 2026-06-09** (submodule commit 4ebc619, deploy run 27206878347 SUCCESS): `same` → обязательная, `compress` → необязательная (с пояснением про хранение без изменений). Проверено на проде через Playwright — список опорных функций теперь соответствует [2]. |
| 10 | `distance` включает упорядоченные сканы / KNN (необязательная) (171) | ✅ ПОДТВЕРЖДЕНО | PG gist.html [2] | `distance` требуется только если opclass содержит ordering operators; включает KNN. Статус «необязательная» — корректен. (Ошибка в #9 касается compress↔same, не distance.) |
| 11 | Для пространственных типов union строит охватывающий прямоугольник, picksplit распределяет прямоугольники, consistent отсекает поддеревья по bbox (173) | ✅ ПОДТВЕРЖДЕНО | PostGIS источники [6]; PG [2] | Корректно. |
| 12 | `gist_geometry_ops_2d` — класс по умолчанию для `geometry`, 2D bbox, поддерживает `&&`, `<->` и др.; создаётся обычным `USING GIST (geom)` (179) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual [6]; KNN doc [8] | Класс по умолчанию для geometry — 2D; `<->` (KNN 2D) обслуживается им. Корректно. |
| 13 | `gist_geometry_ops_nd` — n-мерный класс для `geometry`, оператор `&&&`, указывается явно (180–186) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual [6] | `gist_geometry_ops_nd` + оператор `&&&` (n-D overlap) — корректно; синтаксис явного указания opclass верен. |
| 14 | `gist_geography_ops` — класс по умолчанию для `geography`, поддерживает `&&` (188) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual [6] | Корректно. |
| 15 | Таблица opclass→оператор (2d→&&,<->; nd→&&&; geography→&&), помечена иллюстративной со ссылкой на [6] (192–198) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual [6] | Маппинг не ошибочен; хедж «таблица иллюстративная, полный перечень см. [6]» уместен. |
| 16 | Если оператор не входит в opclass индекса — индекс не используется (190, 248) | ✅ ПОДТВЕРЖДЕНО | PG planner / opclass семантика [5] | Корректно. |
| 17 | `<->` возвращает 2D-расстояние между геометриями (202) | ✅ ПОДТВЕРЖДЕНО | PostGIS geometry_distance_knn.html [8] | "returns the 2D distance between A and B". Корректно. |
| 18 | `<->` в `ORDER BY ... LIMIT k` обслуживается индексом благодаря опорной функции `distance`; обход извлекает кандидатов по возрастанию расстояния (204) | ✅ ПОДТВЕРЖДЕНО | PG gist.html [2]; PostGIS KNN [8] | Index-assisted KNN через distance support method. Корректно. |
| 19 | Условие 1: сравниваемая точка должна быть КОНСТАНТОЙ (не из подзапроса/CTE) (217) | ✅ ПОДТВЕРЖДЕНО | PostGIS KNN [8] | "Index only kicks in if one of the geometries is a constant (not in a subquery/cte)." Дословно. |
| 20 | Условие 2: `<->` должен стоять в `ORDER BY`, в `WHERE` индексом не ускоряется (218) | ✅ ПОДТВЕРЖДЕНО | PostGIS KNN [8] | Оператор используется индексом только в ORDER BY. Корректно. |
| 21 | На обходе индекс оперирует расстоянием до bbox, точное расстояние уточняется на кандидатах с перепроверкой (recheck) (220) | ✅ ПОДТВЕРЖДЕНО | PostGIS KNN [8] (PG 9.5+) | Для PG 9.5+ индекс даёт нижнюю границу по bbox, true distance пересчитывается с recheck. Корректно. |
| 22 | Пример SRID 26918 (UTM), ST_SetSRID(ST_MakePoint(...)) для 5 ближайших станций (206–213) | ✅ ПОДТВЕРЖДЕНО | nyc workshop dataset (SRID 26918) | Набор данных NYC использует SRID 26918 (UTM zone 18N). Синтаксис корректен. |
| 23 | `ST_Distance(geom,:point) < 500` НЕ использует индекс — функция вычисляется на строках, не даёт `&&` (226–234) | ✅ ПОДТВЕРЖДЕНО | PostGIS ST_DWithin doc [9]; Workshop [7] | ST_Distance не добавляет bbox-условие; не index-aware. Корректно. |
| 24 | `ST_DWithin` использует индекс — добавляет bbox-условие `&&` (геометрия против прямоугольника, расширенного на радиус), затем точная проверка (234–242) | ✅ ПОДТВЕРЖДЕНО | PostGIS ST_DWithin doc [9] | "This function call includes a bounding box comparison that makes use of any indexes." Корректно — ключевое отличие от ST_Distance передано верно. |
| 25 | Планировщик может выбрать seq scan при низкой селективности / маленькой таблице (244) | ✅ ПОДТВЕРЖДЕНО | PG using-explain.html / planner [5] | Корректно (стандартное cost-based поведение). |
| 26 | Устаревшая статистика → неоптимальный план; решение ANALYZE; PostGIS поддерживает собственную статистику геометрии и собственные оценщики селективности (246) | ✅ ПОДТВЕРЖДЕНО | PostGIS Manual [6]; PG ANALYZE | PostGIS регистрирует кастомные selectivity/cost-функции для пространственных операторов и собирает geometry-статистику. Прежняя непроверяемая формулировка про «N-D гистограмму» снята — корректно, неподтверждаемых утверждений о селективности не осталось. |
| 27 | Функциональное преобразование столбца (`ST_Transform(geom,4326)`) ломает обычный индекс; помогает только функциональный индекс по тому же выражению (250–254) | ✅ ПОДТВЕРЖДЕНО | PG expression indexes; PostGIS practice | Корректно — стандартное поведение expression indexes. |
| 28 | SP-GiST — фреймворк для несбалансированных деревьев на разбиении пространства (quadtree, k-d, radix); разбиения НЕ перекрываются; хорош для точечных данных (256–260) | ✅ ПОДТВЕРЖДЕНО | PG spgist.html [3] | Корректно: SP-GiST = space-partitioned, неперекрывающиеся разбиения, для точечных/непересекающихся данных. |
| 29 | Для `point`: классы `quad_point_ops` (quadtree, по умолчанию) и `kd_point_ops` (k-d), оба поддерживают KNN через `<->` (260) | ✅ ПОДТВЕРЖДЕНО | PG spgist.html [3] | Оба opclass для point существуют; `quad_point_ops` — default; оба поддерживают ordering (`<->`) KNN. Корректно. |
| 30 | BRIN — блочно-диапазонный; для геометрии хранит охватывающий bbox диапазона страниц; lossy, отсекает диапазоны + recheck; выигрывает на больших физически отсортированных по близости таблицах (262) | ✅ ПОДТВЕРЖДЕНО | PG brin.html [4]; PostGIS Manual [6] | Корректно. |
| 31 | В ядре PG геометрические типы — класс `box_inclusion_ops`; PostGIS дополнительно предоставляет BRIN-классы для `geometry` (262) | ✅ ПОДТВЕРЖДЕНО (с уточнением) | PG brin.html [4]; PostGIS Manual [6] | Ядро PG: `box_inclusion_ops` для типа `box` — верно. PostGIS: `brin_geometry_inclusion_ops_2d/_3d/_4d` (через `USING BRIN (geom)`). Утверждение хеджировано корректно, не переоценено. |
| 32 | Резюме: GiST по умолчанию; SP-GiST быстрее на точечных непересекающихся; BRIN на огромных естественно-отсортированных таблицах (264) | ✅ ПОДТВЕРЖДЕНО | агрегирует [3][4][6] | Корректное обобщение. |
| 33 | Формула стоимости двухфазной модели: фильтрация ~ высота дерева (логарифм); уточнение ~ число_кандидатов × стоимость предиката (266–276) | ✅ ПОДТВЕРЖДЕНО | согласуется с моделью R-дерева | Качественная модель верна; обход GiST по высоте логарифмичен, refine линеен по кандидатам. Формулы в backticks, не LaTeX. |
| 34 | Список литературы: 9 ссылок с anchor-id, метаданные | ✅ ПОДТВЕРЖДЕНО | каждый URL проверен | Все 9 ссылок резолвятся; авторы/конференция/год для [1] (VLDB 1995) верны; doc-ссылки PG/PostGIS актуальны. |

## Сводка

- ❌ ОПРОВЕРГНУТО: 0 (было 1 — строка #9, ИСПРАВЛЕНА в этом же проходе).
- ⚠️ НЕТОЧНО: 0.
- 🔍 НЕ УДАЛОСЬ ПОДТВЕРДИТЬ: 0.
- ✅ ПОДТВЕРЖДЕНО: 33 + 1 исправлено = 34.

### Разрешение замечаний

- Строка #9 (была ❌): статус обязательности `compress`/`same` исправлен минимальной прямой правкой evaluator'а (commit 4ebc619), пересобрано Node 20 (build SUCCESS, onBrokenLinks:'throw'), передеплоено (run 27206878347 SUCCESS), верифицировано на проде через Playwright (cache-busted). Замечание ЗАКРЫТО. Силентных дропов нет.
